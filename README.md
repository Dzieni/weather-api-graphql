# WeatherAPI.com GraphQL server

## Getting started

- Set up needed variables in `.env`:
```env
WEATHERAPI_KEY=yourKey
OIDC_CLIENT_ID=yourClientId
OIDC_COOKIE_SECRET=yourSecret
OIDC_ISSUER_BASE_URL=yourBaseUrl
```
- `npm i`
- `npm start`
